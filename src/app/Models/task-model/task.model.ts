export class Task {
    id: number;
    listId: number;
    todo: string;
    description: string;
    priority: string;
    dueDate: Date;
    status: string;
    complete: boolean;
}

enum Prority {
    
}

enum Status {
    
}
