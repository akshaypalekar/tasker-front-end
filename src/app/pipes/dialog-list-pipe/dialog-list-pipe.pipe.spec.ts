import { DialogListPipePipe } from './dialog-list-pipe.pipe';

describe('DialogListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DialogListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
