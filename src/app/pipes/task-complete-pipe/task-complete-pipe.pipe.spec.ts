import { TaskCompletePipePipe } from './task-complete-pipe.pipe';

describe('TaskCompletePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskCompletePipePipe();
    expect(pipe).toBeTruthy();
  });
});
